import React from "react";
import { Outlet } from "react-router-dom";

const DailySales = () => {
  return (
    <React.Fragment>
      <Outlet />
    </React.Fragment>
  );
};

export default DailySales;
